// PKL-103: `read?(uri)` returns `null` for a missing resource, but
// preserves sandbox policy failures so rejected schemes still surface
// as diagnostics.
maybe_missing = read?("env:NONEXISTENT_XYZ_VAR")

maybe_bad_scheme = module.catch(() -> read?("https://example.com/foo"))
